# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=golapp

# JWT secret para tokens
JWT_SECRET=change-this-secret

# Puerto opcional para el servidor Express
# PORT=3000

# Configuración SMTP (opcional) para notificaciones por mail
# SMTP_HOST=
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="GolApp <notificaciones@golapp.com>"
# SMTP_ALLOW_INVALID=false
